/**
 * Cross-Platform Service Manager
 * Re-exports from @daemux/service workspace package
 */

export { ServiceManager, getServiceManager } from '@daemux/service';
export type { Platform, ServiceConfig, ServiceInfo, ServiceStatus, Logger, PlatformServiceManager } from '@daemux/service';
